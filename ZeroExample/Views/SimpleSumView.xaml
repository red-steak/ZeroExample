<UserControl x:Class="ZeroExample.Views.SimpleSumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZeroExample.Views"
             mc:Ignorable="d" 
             Margin="8"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Expander Header="{Binding Header}" Padding="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid DockPanel.Dock="Top" Margin="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Width="120" TextAlignment="Center" Text="{Binding GroupByHeader}" Margin="0,0,5,0"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Width="120" TextAlignment="Center" Text="Price" Margin="0,0,5,0" FontWeight="Regular"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Width="120" TextAlignment="Center" Text="Price with VAT" Margin="0,0,5,0" FontWeight="Regular"/>
                </Grid>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Totals}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="1,4"
                                UseLayoutRounding="True"
                                BorderThickness="1"
                                CornerRadius="4"
                                BorderBrush="AntiqueWhite"
                                Background="Transparent">
                                <Grid Margin="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Width="120" Padding="2" TextAlignment="Center" FontSize="12" FontWeight="Bold" Text="{Binding Model}" />
                                    <TextBlock Grid.Row="1" Grid.Column="0" Width="120" Padding="2" TextAlignment="Right" FontSize="12" FontWeight="Regular" Text="{Binding Price, Converter={StaticResource CurrencyConverter}}" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Width="120" Padding="2" TextAlignment="Right" FontSize="12" FontWeight="Regular" Text="{Binding PriceWithVat, Converter={StaticResource CurrencyConverter}}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Expander>
    </Grid>
</UserControl>
